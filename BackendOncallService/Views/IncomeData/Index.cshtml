@model IEnumerable<BackendOncallService.Models.OncallCell>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OncallName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OncallShift)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OncallDate)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OncallName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OncallShift)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OncallDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
